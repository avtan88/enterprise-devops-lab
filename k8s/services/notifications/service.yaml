apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: enterprise-devops
spec:
  selector:
    app: notifications
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
