apiVersion: v1
kind: Service
metadata:
  name: payments-service
  namespace: enterprise-devops
spec:
  selector:
    app: payments
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
