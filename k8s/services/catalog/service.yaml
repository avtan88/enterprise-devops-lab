apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: enterprise-devops
spec:
  selector:
    app: catalog
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
