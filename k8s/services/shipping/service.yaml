apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: enterprise-devops
spec:
  selector:
    app: shipping
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
