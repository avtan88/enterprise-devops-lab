apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: enterprise-devops
spec:
  selector:
    app: billing
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
