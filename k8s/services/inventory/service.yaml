apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: enterprise-devops
spec:
  selector:
    app: inventory
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
