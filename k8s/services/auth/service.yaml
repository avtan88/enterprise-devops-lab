apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: enterprise-devops
spec:
  selector:
    app: auth
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
