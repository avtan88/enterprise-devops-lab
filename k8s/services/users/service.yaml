apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: enterprise-devops
spec:
  selector:
    app: users
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
